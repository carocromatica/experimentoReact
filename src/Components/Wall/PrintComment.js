import React from 'react';

const Comentario = (props) => (
    
        <div className="contenido">
           <h2>{props.texto} </h2>
        
        </div>
    );

export default Comentario;